import styles from '../css/ProfileInfo.module.css'

const ProfileInfo = () => {
    return ( 
    <div className={`
           ${styles.containerWrapper}
           container
           `}>
           <p>Profile info</p>
         </div>
        
    );
}
 
export default ProfileInfo;